apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: test-cluster
  labels:
    dashboards: "grafana"
    folders: "grafana"
spec:
  config:
    security:
      admin_user: root
      admin_password: start
    log:
      mode: "console"
    auth:
      disable_login_form: "false"
